{
  "name": "coldtrace-app-web-json-server",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "json-server --watch db.json --routes routes.json --port 3000",
    "start": "json-server --host 0.0.0.0 --port ${PORT:-3000} db.json --routes routes.json"
  },
  "dependencies": {
    "json-server": "0.17.4"
  }
}
